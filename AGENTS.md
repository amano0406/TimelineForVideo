# AGENTS.md

## Repo purpose

This repository is `TimelineForVideo`.
Its job is to turn local video files into timeline-oriented artifacts that combine speech and screen evidence for human review and LLM handoff.

## Must preserve

- Keep the product local-first.
- Treat CLI / worker / output artifacts as the source of truth. This repo is CLI-only.
- Do not delete, overwrite, mass-move, or mass-rename original input videos.
- Keep gaps, warnings, partial failure, and fidelity loss visible instead of hiding them.
- Preserve the current run/output contract unless a breaking change is explicitly approved.
- Prefer fixed input/output/state roots by default rather than arbitrary one-off path workflows.

## Product-specific guardrails

- Preserve transcript, screen evidence, timeline outputs, and export package consistency.
- Keep reuse/rerun behavior understandable and documented when changing related code.
- Do not silently collapse screen-derived evidence into plain summary text without keeping traceability.
- Keep Docker / `docker compose` / `start.bat` reproducible.
- Do not reintroduce a web UI or browser-based operator console unless explicitly requested.

## Standard model

```text
InputSource = configured input source
InputItem   = detected processable input from InputSource
Job         = processing request based on an InputItem snapshot and profile
Run         = one execution attempt for a Job
Artifact    = output generated by a Run
```

## Safe work without extra confirmation

- read-only investigation
- `AGENTS.md`, README, docs, and `.env.example` updates
- non-destructive small code fixes
- CLI and Docker reproducibility fixes
- lightweight local tests or smoke checks
- Docker build / compose smoke checks
- output contract consistency fixes that do not remove user data

## Ask before doing these

- deleting or rewriting raw input videos
- mass move / mass rename of user files
- breaking output contract changes
- repo or product rename
- new hosted/cloud dependency
- deploy, external posting, or secret changes
- broad architecture rewrites

## Before finishing

- Read the README and relevant docs first.
- Keep current output shape stable unless the task explicitly changes it.
- Update docs when behavior or contracts change.

## Report format

```md
## Current state
## Completed
## Changed files
## Fixed input/output policy check
## Job / Run model check
## Tests
## Risks
## Next safe tasks
## Human decisions needed
```
